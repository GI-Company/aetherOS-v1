
package aetherscript

import (
	"go/ast"
)

// Compile takes an AST and compiles it into an executable format.
func Compile(node *ast.File) ([]byte, error) {
	// TODO: Implement the AetherScript compiler.
	return nil, nil
}
